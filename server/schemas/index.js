// Importing type definitions and resolvers for GraphQL schema
const typeDefs = require('./typeDefs');
const resolvers = require('./resolvers');

// Exporting type definitions and resolvers as an object
module.exports = { typeDefs, resolvers };